<?php foreach($ListRequests as $request): ?>
    <h1>Cantidad de copias:<?= $request->cantidad ?></h1>
    <h1>Precio total:<?= $request->precio ?></h1>
    <?php if($this->user): ?>
    <a href="eliminarRequest/<?= $request->id_pedido?>" type="button" class='btn btn-danger btn-sm ml-auto'>Borrar</a>
    <?php endif ?>
<?php endforeach; ?>
